/*using System;

namespace problemas
{
    public class Arrays
    {
        public static void Main (string[] arg){
            string[] carros = { "fusca", "Brasilia", "Onix", "Volvo"};
            int[] numeros = { 1, 2, 3, 4 };

            int posicao = int.Parse(Console.ReadLine());

            Console.WriteLine( "o que se encontra na posição esolhida é: " +carros[posicao]);
            Console.WriteLine("o nosso Array tem: " + carros.Length + " elementos");

            for (int i = 0; i < carros.Length; i++)
            {
                Console.WriteLine(carros[i]);
            }
        }
    }
}*/