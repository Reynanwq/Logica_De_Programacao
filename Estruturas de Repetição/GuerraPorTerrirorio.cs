/*using System;

namespace problemas
{
    public class GuerraPorTerrirorio
    {
        public static void main(string[] arg){
            int n = int.Parse(Console.ReadLine());
            int res1 = 0;
            int inutil = n - 1;

            while (inutil < n)
            {
                int v = int.Parse(Console.ReadLine());
                inutil = inutil + 1;

                int sum = v.Sum();
            }
            //int sum = v.sum(v);
            int res = (v / 2);
            int passo = 0;

            for (int i = 0; i < v; i++)
            {
                if (passo == res)
                {
                    res = key
                }
                passo = passo + value;
            }
            Console.WriteLine(res);
        }
    }
}*/